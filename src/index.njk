---
layout: base.njk
title: Prayer Times
---

<div class="flex justify-center items-center h-screen bg-black" x-data="{items: times}">
    <table class="table-fixed">
        <tbody class="text-white">
            <template x-for="item in items" :key="item.name">
                <tr>
                    <th class="text-right p-3" scope="row" x-text="item.name"></th>
                    <td x-text=item.time></td>
                </tr>
            </template>
        </tbody>
    </table>
</div>
